protocollo REST
	visione WEB incentrata sulla risorse
	insieme di risorse sulle quali i client possono chiedere operazioni classiche del protocollo HTTP
	RESOURCE ORIENTED ARCHITECTURE

protocollo SOAP
	(Simple Object Access Protocol)
	incentrato sui servizi
	metodi richiamabili da remoto
	SERVICE ORIENTED ARCHITECTURE

@Scope(ConfigurableBeanFactory.SCOPE_SINGLETON)
	un solo oggetto per tutta l'applicazione

@Scope(ConfigurableBeanFactory.SCOPE_PROTOTYPE)
	on demand, usa e getta



colloquio {
	il 2 mattina verso mezzogiorno
	jbpm di jboss
	sytel x vodaphone
}

differenza @Component @Service da spring
@Named e primeface
mai utilizzato web service ?
differenza tra hibernate e jpa
differenza tra weblogic e tomcat
differenza livelli cache di hibernate

differenza GET e POST
singleton
ajax
	asincrono aggiornamento dati in pagina senza ricaricarla tutta
grep
polimorfismo
synchronized in java
	synchronized means that in a multi threaded environment, an object having  synchronized method(s)/block(s) does not let two threads to access the 	synchronized method(s)/block(s) of code at the same time. This means that one thread can't read while another thread updates it.

19gen17
	differenza private public protected e la default qual'è
	[liferay]differenza nel service builder tra servizi interni/esterni
	memoria virtual machine di java, com'è gestita
	application servers, quale ne conosci